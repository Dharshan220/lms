{
  "name": "nano-spark-lms",
  "version": "1.0.0",
  "description": "Nano Spark LMS - IoT, Robotics & AI Learning Platform",
  "scripts": {
    "build": "php artisan view:clear && php artisan config:cache && php artisan route:cache",
    "postbuild": "echo 'Build complete'"
  },
  "engines": {
    "node": "18.x"
  }
}
